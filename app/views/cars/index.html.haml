%h1.text-center Available cars
%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Class
      %th{:colspan => "3"}
  %tbody
    - @cars.each do |car|
      %tr
        %td= car.name
        %td= car.kind
        %td= link_to 'Rent', new_car_rental_path(car), class: 'btn btn-success'
        %td
          = link_to 'Show', car, class: 'btn btn-default'
          - if user_signed_in?
            = link_to 'Edit', edit_car_path(car), class: 'btn btn-info'
            = link_to 'Destroy', car, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger'
- if user_signed_in?
  = link_to 'New Car', new_car_path, class: 'btn btn-success pull-right'
