%h1.text-center
  Editing Rental:
  = @rental.car.name

= render 'form'
.pull-right
  = link_to 'Show', car_path(@rental.car), class: 'btn btn-info'
  = link_to 'Back', car_path(@rental.car), class: 'btn btn-default'
